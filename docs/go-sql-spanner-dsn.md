# go-sql-spanner connection properties reference

This document summarizes **go-sql-spanner connection properties** and how they appear in both the connection string (DSN) and client-side `SHOW` / `SET` / `RESET` statements. **Upstream is always canonical.**

| Reference | URL |
|-----------|-----|
| Module (version, API) | [pkg.go.dev/github.com/googleapis/go-sql-spanner](https://pkg.go.dev/github.com/googleapis/go-sql-spanner) |
| Repository README and samples | [github.com/googleapis/go-sql-spanner](https://github.com/googleapis/go-sql-spanner) |
| Cloud Spanner driver capabilities overview | [docs.cloud.google.com/spanner/docs/drivers-overview](https://docs.cloud.google.com/spanner/docs/drivers-overview) |
| Driver implementation (DSN regex and comments) | `driver.go` around `dsnRegExp` / `dsnRegExpString` |

This repo pins a version in `go.mod` and references **`github.com/googleapis/go-sql-spanner`** `connection_properties.go`.

- A **machine-readable list** of names, Context, and English descriptions is **[generated/connection-properties.generated.md](./generated/connection-properties.generated.md)** (generated by `go generate ./...` / `tools/genprops`).
- The **tables below are a hand-written summary** for readability; the generated file and upstream source are the completeness baseline.

New driver versions may add or change parameters—verify behavior in **upstream release notes ([CHANGES.md](https://github.com/googleapis/go-sql-spanner/blob/main/CHANGES.md)) and source**.

---

## How this relates to spannersh

**spannersh** delegates execution to **go-sql-spanner** via Go's `database/sql` integration. That means driver features documented in the official Cloud Spanner [drivers overview](https://docs.cloud.google.com/spanner/docs/drivers-overview) for the Go `database/sql` path are generally available here through the driver, rather than being re-implemented in this repository.

This document is therefore mainly about the **driver property model** that **spannersh** exposes through:

- `--dsn-suffix` at startup
- client-side `SHOW` / `SET` / `RESET` statements after connect

---

## One property namespace; DSN is one input path

go-sql-spanner holds per-connection state as **connection properties** (defined in `connection_properties.go`).

- The **`name`** in DSN `name=value` is the connection property identifier.
- **`SHOW …` / `SET …` / `RESET …`** client-side statements use the **same property names**. Syntax details (e.g. GoogleSQL requires `SHOW VARIABLE …`) are in **[go-sql-spanner-client-side-statements.md](./go-sql-spanner-client-side-statements.md)**.
- **`--dsn-suffix`** in **spannersh** is just a way to append these same property names to the startup DSN.

So **DSN keys** and **SHOW/SET names** share one namespace. Value formats and meanings are shared between DSN and runtime settings; what changes is **when** a property can be applied:

- **`ContextStartup`** properties must be set when opening the connection, typically through the DSN / `--dsn-suffix`.
- **`ContextUser`** properties are session/runtime settings and can generally be inspected with `SHOW` and updated with `SET` / `RESET`.

Check each property's `ContextStartup` / `ContextUser` and description in upstream source.

---

## DSN shape (one way to set startup properties)

The connection name consists of (summarized from `driver.go`):

1. **(Optional) host** — `host:port/` for emulator or custom endpoints.
2. **Database path** — `projects/PROJECT/instances/INSTANCE/databases/DATABASE` (the regex may allow abbreviated forms).
3. **(Optional) parameters** — after `?` or `;`, `name=value` pairs separated by `;`.

Example (similar to README):

```text
localhost:9010/projects/test-project/instances/test-instance/databases/test-database;use_plain_text=true;disable_route_to_leader=true
```

### Parameter naming

Internally properties use **`snake_case`** (e.g. `use_plain_text`). The reader also treats **names without underscores** (e.g. `useplaintext`) as the same property. README may show historical **camelCase**; **`snake_case`** is the reliable form.

---

## Connection properties (summary)

The **full table (English descriptions, Context)** is [generated/connection-properties.generated.md](./generated/connection-properties.generated.md). **DSN parameter names** and **`SHOW` / `SET` / `RESET`** identifiers share the same namespace.

Properties with `connectionstate.ContextStartup` apply at connect time; `ContextUser` properties are session/runtime settings. See each property’s English `description` in source.

### Connection, startup, TLS, auth

| Property (DSN key / SHOW·SET name) | Summary |
|------------------------------------|---------|
| `endpoint` | Target endpoint; interacts with defaults when `auto_config_emulator=true`, etc. |
| `authority` | Expected server name for TLS. |
| `credentials` | Path to service account JSON; ADC if unset. |
| `credentials_json` | Inline JSON credentials. |
| `use_plain_text` | Non-TLS (e.g. emulator) when `true`. |
| `disable_native_metrics` | Disable native metrics. |
| `num_channels` | gRPC channel count; `0` means default. |
| `database_role` | Database role. |
| `disable_route_to_leader` | Disable routing to leader. |
| `enable_end_to_end_tracing` | End-to-end tracing. |
| `statement_cache_size` | Statement parser cache size; see `SPANNER_DEFAULT_STATEMENT_CACHE_SIZE`. |
| `disable_statement_cache` | Disable statement cache. |
| `connect_timeout` | Timeout when creating a connection. |
| `is_experimental_host` | Experimental host connection. |
| `ca_cert_file` / `client_cert_file` / `client_cert_key` | mTLS for experimental hosts. |
| `min_sessions` / `max_sessions` | **Deprecated** (session pool removal). |
| `dialect` | Hint when **creating** a new DB; existing DBs are usually auto-detected. |
| `database_dialect` | **Read-only** — dialect of the **connected** database (determined at connect). Distinct from `dialect`; inspect with **`SHOW VARIABLE database_dialect`** (or see the generated property list). |
| `auto_config_emulator` | Emulator auto-setup, instance/DB creation, etc. |

### Transactions, statements, decode, DDL

| Property (DSN key / SHOW·SET name) | Summary |
|------------------------------------|---------|
| `connection_state_type` | Connection state type (`NON_TRANSACTIONAL` / `TRANSACTIONAL`, etc.). |
| `isolation_level` | Default isolation level. |
| `read_lock_mode` | Read lock mode (`PESSIMISTIC` / `OPTIMISTIC`, etc.). |
| `begin_transaction_option` | `Inlined` / `Explicit`, etc. |
| `autocommit_dml_mode` | Auto-commit DML mode. |
| `retry_aborts_internally` | Retry on abort. |
| `read_only_staleness` | Staleness for one-shot queries / read-only transactions. |
| `auto_partition_mode` | Run as partitioned queries. |
| `data_boost_enabled` | Data Boost for partitioned queries. |
| `max_partitions` / `max_partitioned_parallelism` | Partitioned query hints. |
| `auto_batch_dml` / `auto_batch_dml_update_count` / `auto_batch_dml_update_count_verification` | DML batching. |
| `rpc_priority` | RPC priority (`HIGH` / `MEDIUM` / `LOW`, etc.). |
| `optimizer_version` / `optimizer_statistics_package` | Optimizer settings. |
| `decode_to_native_arrays` / `decode_numeric_to_string` / `send_typed_strings` | Decode/bind behavior. |
| `ddl_execution_mode` / `ddl_async_wait_timeout` | DDL execution mode. |
| `transaction_read_only` / `transaction_deferrable` / `exclude_txn_from_change_streams` | Transaction-related. |
| `transaction_tag` / `max_commit_delay` / `commit_priority` / `return_commit_stats` / `transaction_batch_read_only` / `transaction_timeout` | Commit, tags, timeouts. |
| `statement_tag` / `statement_timeout` | Per-statement tag and timeout. |

### Read-only (mainly observable via DSN)

| Property (DSN key / SHOW·SET name) | Summary |
|------------------------------------|---------|
| `database_dialect` | **Read-only** — GoogleSQL vs PostgreSQL for the database you opened (see also `dialect` above). |
| `commit_timestamp` / `commit_response` | Last commit info (read-only). |

### All properties (alphabetical)

The grouped tables above are for quick orientation. The table below lists **every** DSN / `SHOW` / `SET` identifier exposed in **`github.com/googleapis/go-sql-spanner`** for the version pinned in this repository’s `go.mod` (same set as [generated/connection-properties.generated.md](./generated/connection-properties.generated.md)). Summaries are shortened from upstream English; for verbatim **`description`** text, see the generated file. **When you bump `go-sql-spanner`**, run `go generate ./...` and update this table so its property names and summaries stay aligned with the generated file (adjust wording if you intentionally shorten further).

| Property | Context | Summary |
|----------|---------|---------|
| `authority` | `ContextStartup` | The expected server name in the TLS handshake. By default, the endpoint hostname is used. This option is useful when connecting to Spanner via Google Private Connect or other custom endpoints where… |
| `auto_batch_dml` | `ContextUser` | Automatically buffer DML statements that are executed on this connection and execute them as one batch when a non-DML statement is executed, or when the current transaction is committed. The update… |
| `auto_batch_dml_update_count` | `ContextUser` | DML statements that are executed when auto_batch_dml is set to true, are not directly sent to Spanner, but are buffered in the client until the batch is flushed. This property determines the update… |
| `auto_batch_dml_update_count_verification` | `ContextUser` | The update count that is returned for DML statements that are buffered during an automatic DML batch is by default 1. This value can be changed by setting the connection variable auto_batch_dml_upd… |
| `auto_config_emulator` | `ContextStartup` | Automatically configure the connection to try to connect to the Cloud Spanner emulator (true/false). The instance and database in the connection string will automatically be created if these do not… |
| `auto_partition_mode` | `ContextUser` | Execute all queries on this connection as partitioned queries. Executing a query that cannot be partitioned will fail. Executing a query in a read/write transaction will also fail. |
| `autocommit_dml_mode` | `ContextUser` | Determines the transaction type that is used to execute DML statements when the connection is in auto-commit mode. |
| `begin_transaction_option` | `ContextUser` | BeginTransactionOption determines the default for how to begin transactions. The driver uses spanner.InlinedBeginTransaction by default for both read-only and read/write transactions. |
| `ca_cert_file` | `ContextStartup` | The path to the CA certificate file to use for TLS connections to the server. This is only needed when connecting to an experimental host endpoint |
| `client_cert_file` | `ContextStartup` | The path to the client certificate file to use for mTLS connections to the server. This is only needed when connecting to an experimental host endpoint |
| `client_cert_key` | `ContextStartup` | The path to the client certificate key file to use for mTLS connections to the server. This is only needed when connecting to an experimental host endpoint |
| `commit_priority` | `ContextUser` | Sets the priority for commit RPC invocations from this connection (HIGH/MEDIUM/LOW/UNSPECIFIED). The default is UNSPECIFIED. |
| `commit_response` | `ContextUser`; read-only | The commit response of the last implicit or explicit read/write transaction that was executed on the connection, or an error if the connection has not executed a read/write transaction that committ… |
| `commit_timestamp` | `ContextUser`; read-only | The commit timestamp of the last implicit or explicit read/write transaction that was executed on the connection, or an error if the connection has not executed a read/write transaction that commit… |
| `connect_timeout` | `ContextStartup` | The amount of time to wait before timing out when creating a new connection. |
| `connection_state_type` | `ContextStartup` | The type of connection state to use for this connection. Can only be set at start up. If no value is set, then the database dialect default will be used, which is NON_TRANSACTIONAL for GoogleSQL an… |
| `credentials` | `ContextStartup` | The location of the credentials file to use for this connection. If neither this property or encoded credentials are set, the connection will use the default Google Cloud credentials for the runtim… |
| `credentials_json` | `ContextStartup` | Service account or refresh token JSON credentials. |
| `data_boost_enabled` | `ContextUser` | Enable data boost for all partitioned queries that are executed by this connection. This setting is only used for partitioned queries and is ignored by all other statements. Either set `auto_partit… |
| `database_dialect` | `ContextUser`; read-only | The dialect of the database that the connection is connected to. This property is read-only and is automatically determined at startup. |
| `database_role` | `ContextStartup` | Sets the database role to use for this connection. The default is privileges assigned to IAM role |
| `ddl_async_wait_timeout` | `ContextUser` | The timeout to wait for a DDL operation to finish when ddl_execution_mode is set to ASYNC_WAIT. The default timeout is 10 seconds. |
| `ddl_execution_mode` | `ContextUser` | ddl_execution_mode determines how DDL statements are executed. SYNC (default): The driver waits for the Long Running Operation (LRO) to complete. ASYNC: The driver returns immediately after the ser… |
| `decode_numeric_to_string` | `ContextUser` | decode_numeric_to_string determines whether numeric values should be returned as strings or as instances of big.Rat. The latter is the default and is consistent with the type that is used by the Sp… |
| `decode_to_native_arrays` | `ContextUser` | decode_to_native_arrays determines whether arrays that have a Go native type should be decoded to those types rather than the corresponding spanner.NullTypeName type. Enabling this option will for … |
| `dialect` | `ContextUser` | The default dialect to use when creating new databases using this connection. You do not need to set this property for any existing database. The driver will automatically detect the dialect of the… |
| `disable_native_metrics` | `ContextStartup` | Disables native metrics for this connection. If true, native metrics will not be emitted. |
| `disable_route_to_leader` | `ContextStartup` | Disable routing read/write transactions and partitioned DML to the leader region (true/false) |
| `disable_statement_cache` | `ContextStartup` | Disables the use of the statement cache. |
| `enable_end_to_end_tracing` | `ContextStartup` | Enable end-to-end tracing (true/false) to generate traces for both the time that is spent in the client, as well as time that is spent in the Spanner server. Server side traces can only go to Googl… |
| `endpoint` | `ContextStartup` | The endpoint that the driver should connect to. The default is the default Spanner production endpoint when auto_config_emulator=false, and the default Spanner emulator endpoint (localhost:9010) wh… |
| `exclude_txn_from_change_streams` | `ContextUser` | exclude_txn_from_change_streams determines whether transactions on this connection should be excluded from change streams. This property affects both normal read/write transactions, DML statements … |
| `is_experimental_host` | `ContextStartup` | Indicates whether the connection is to an experimental host endpoint (true/false). Set this value to true when connecting to an experimental host endpoint |
| `isolation_level` | `ContextUser` | The transaction isolation level that is used by default for read/write transactions. The default is LevelDefault, which means that the connection will use the default isolation level of the databas… |
| `max_commit_delay` | `ContextUser` | The max delay that Spanner may apply to commit requests to improve throughput. |
| `max_partitioned_parallelism` | `ContextUser` | The maximum number of workers to use to read data from partitioned queries. |
| `max_partitions` | `ContextUser` | The max partitions hint value to use for partitioned queries. Set to 0 if you do not want to specify a hint. |
| `max_sessions` | `ContextStartup` | (Deprecated) The maximum number of sessions in the backing session pool. The default is %d. |
| `min_sessions` | `ContextStartup` | (Deprecated) The minimum number of sessions in the backing session pool. The default is %d. |
| `num_channels` | `ContextStartup` | The number of gRPC channels to use to communicate with Cloud Spanner. |
| `optimizer_statistics_package` | `ContextUser` | Sets the query optimizer statistics package to use for this connection. |
| `optimizer_version` | `ContextUser` | Sets the default query optimizer version to use for this connection. |
| `read_lock_mode` | `ContextUser` | This option controls the locking behavior for read operations and queries within a read/write transaction. It works in conjunction with the transaction's isolation level.  PESSIMISTIC: Read locks a… |
| `read_only_staleness` | `ContextUser` | The read-only staleness to use for read-only transactions and single-use queries. |
| `retry_aborts_internally` | `ContextUser` | Should the connection automatically retry Aborted errors (true/false). |
| `return_commit_stats` | `ContextUser` | return_commit_stats determines whether transactions should request Spanner to return commit statistics. |
| `rpc_priority` | `ContextUser` | Sets the priority for all RPC invocations from this connection (HIGH/MEDIUM/LOW/UNSPECIFIED). The default is UNSPECIFIED. |
| `send_typed_strings` | `ContextUser` | Whether string query parameters are sent as untyped (default) or typed strings (see upstream). |
| `statement_cache_size` | `ContextStartup` | The size of the internal cache that is used for connectors that are created from this connection string. This cache stores the result of parsing SQL statements for query parameters and the type of … |
| `statement_tag` | `ContextUser` | The statement (request) tag to add to the next statement on this connection. |
| `statement_timeout` | `ContextUser` | The timeout to apply to all statements on this connection. Setting the timeout to zero means no timeout. |
| `transaction_batch_read_only` | `ContextUser` | transaction_batch_read_only indicates whether read-only transactions on this connection should use a batch read-only transaction. |
| `transaction_deferrable` | `ContextUser` | transaction_deferrable is a no-op on Spanner. It is defined in this driver for compatibility with PostgreSQL. |
| `transaction_read_only` | `ContextUser` | transaction_read_only is the default read-only mode for transactions on this connection. |
| `transaction_tag` | `ContextUser` | The transaction tag to add to the next read/write transaction on this connection. |
| `transaction_timeout` | `ContextUser` | The timeout to apply to all read/write transactions on this connection. Setting the timeout to zero means no timeout. |
| `use_plain_text` | `ContextStartup` | Use a plain text communication channel (i.e. non-TLS) for communicating with the server (true/false). Set this value to true for communication with the Spanner emulator. |

---

## Emulator

- See the [Emulator section](https://github.com/googleapis/go-sql-spanner#emulator) in the go-sql-spanner README for `SPANNER_EMULATOR_HOST` (e.g. `localhost:9010`).
- `auto_config_emulator=true` can auto-create instance/DB on the emulator ([Automatically Create Instance and Database](https://github.com/googleapis/go-sql-spanner#automatically-create-instance-and-database-on-the-emulator)).

---

## How spannersh passes properties into the driver

The shell builds `projects/.../databases/...` from **`-p` / `-i` / `-d`** and appends **`name=value`** pairs from **`--dsn-suffix`**, separated by **`;`**.

```text
spannersh -p PROJECT -i INSTANCE -d DATABASE \
  --dsn-suffix 'use_plain_text=true;connect_timeout=30s'
```

The suffix is concatenated verbatim into the DSN. So `--dsn-suffix` is not a separate configuration system—it is simply a way to provide **go-sql-spanner connection properties** at startup. Inside the shell you can also use **`SHOW VARIABLE …` / `SET …`** with the same property names (syntax: [go-sql-spanner-client-side-statements.md](./go-sql-spanner-client-side-statements.md)).
